name: Test standard image
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    # You need to use the INSTALLATION_NAME from the previous step
    runs-on: github-arc-runners
    steps:
    - run: echo "🎉 This job uses GitHub provided runner image"
